// Re-uses the same viewer; subject is selected via URL.
// For now the viewer manages its own active-subject state and the URL is a
// deep link that the user can copy. Real wiring lands with the API.
export { default } from "../page";
